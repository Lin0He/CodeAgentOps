def detect_anomaly_window(values, threshold, window_size):
    """
    Return the start index of the first rolling window whose average
    is strictly greater than threshold.

    Return -1 if no such window exists.
    """
    return -1
